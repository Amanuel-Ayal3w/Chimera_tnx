[project]
name = "project-chimera"
version = "0.1.0"
description = "Project Chimera autonomous influencer network tooling and tests"
requires-python = ">=3.12"
dependencies = [
    "pytest>=8.0.0",
]

[tool.uv]
managed = true
